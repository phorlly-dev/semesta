import 'package:flutter/material.dart';

class Watches extends StatelessWidget {
  const Watches({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Welcome to watches'));
  }
}
